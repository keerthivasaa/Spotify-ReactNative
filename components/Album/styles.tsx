import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: 200,
        margin: 10
    },
    imageUri: {
        width: '100%',
        height: 200,
    },
    artistHeadline: {
        color: 'grey',
        marginTop: 10
    }
})

export default styles;